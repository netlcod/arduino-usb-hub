name=HubCommand
version=1.0.0
author=arduino-hub
maintainer=arduino-hub
sentence=PC to Arduino HID command channel: transports and device command handlers.
paragraph=Transport abstraction (Feature/Output report over the patched core HID library) plus device-specific command handlers. Command opcodes and payload layouts are generated from the arduino-hub command schema (commands/*.json) into mouse_commands.h.
category=Communication
url=
architectures=avr
